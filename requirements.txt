# MetaScan v2.1 Enhanced - Python Dependencies
# Core Framework
Flask==3.0.3
Werkzeug==3.0.3

# Image Processing (Required for metadata removal and download)
Pillow==10.4.0

# Computer Vision (Optional - for QR code detection)
opencv-python-headless==4.10.0.84

# File Type Detection (Optional)
python-magic==0.4.27
python-magic-bin==0.4.14; sys_platform == "win32"

# Numerical Computing (Optional - for steganography detection)
numpy==1.26.4

# Geographic Data (Optional - for GPS features)
geopy==2.4.1

# Additional File Type Support
filetype==1.2.0

# Development Dependencies (Optional)
pytest==8.3.2
black==24.4.2
flake8==7.1.0

# Note: All optional dependencies will gracefully degrade if not available
# The core functionality (metadata removal + download) only requires Flask and Pillow
